# Package

version       = "0.0.1"
author        = "Eric Berquist"
description   = "Tools for working with collections of ordered ranges and spans"
license       = "MIT"
srcDir        = "src"

# Dependencies

requires "nim >= 1.0.0"
requires "gara >= 0.2.0"
requires "sorta >= 0.2.0"
